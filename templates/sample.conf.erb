input {
  file {
    path => '/var/log/httpd/access_log'
  }
}

filter {
  grok {
    match => { 'message' => '%{COMBINEDAPACHELOG}' }
  }

  useragent {
    source => 'agent'
  }
}

output {
  elasticsearch { }
}
